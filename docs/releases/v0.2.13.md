## Summary

- Clarifies the app workflow by splitting the workspace into `Step 1 · Source`, `Step 2 · Configure`, and `Step 3 · Run & Review`.
- Moves report export controls into the Latest Result report area so live monitoring stays focused on run output.
- Adds export ownership and accessibility regression coverage for success and failure notices.
- Improves export notice announcement semantics and wrapping for long report paths.
- Bumps Load Rift metadata to version 0.2.13.

## Downloads

- macOS Apple Silicon DMG: attached to this GitHub release as `Load.Rift_0.2.13_aarch64.dmg`
- macOS Intel DMG: attached to this GitHub release as `Load.Rift_0.2.13_x64.dmg`
- Linux AppImage: attached to this GitHub release as `Load.Rift_0.2.13_amd64.AppImage`
- Linux `.deb`: attached to this GitHub release as `Load.Rift_0.2.13_amd64.deb`
- Linux `.rpm`: attached to this GitHub release as `Load.Rift-0.2.13-1.x86_64.rpm`
- APT setup script: attached to this GitHub release as `install-apt-repo.sh`
- APT setup package: attached to this GitHub release as `load-rift-repository-setup_1.0_all.deb`

## Linux APT Install

Enable the Load Rift APT repository once:

```bash
bash <(curl -fsSL https://github.com/ddv1982/load-rift/releases/latest/download/install-apt-repo.sh)
```

Then install or update normally:

```bash
sudo apt update
sudo apt install load-rift
```

The setup script authenticates the repository setup package checksum with a detached GPG signature and a release-pinned signer fingerprint before installing the repository configuration.

## Bundled Third-Party Software

This package bundles Grafana k6 v2.0.0, licensed under AGPL-3.0-only.
Corresponding source: https://github.com/grafana/k6/tree/v2.0.0
Source archive: https://github.com/grafana/k6/archive/refs/tags/v2.0.0.tar.gz
Additional bundled licensing notices are included in the package.

## Verification

- [x] `npm test -- src/app/App.validation.test.tsx`
- [x] `npm run typecheck`
- [x] `npm run lint`
- [x] `npm run build`
- [x] `npm test`
- [x] `cd src-tauri && cargo test && cargo check`

## Lore

Constraint: Release notes are read by the GitHub Actions release workflow from `docs/releases/v0.2.13.md`.
Constraint: The release preserves the existing backend-retained report export behavior even when the UI has no current result object.
Rejected: Keeping export controls in the live run monitor | it mixed completed-report actions into the live output area and weakened the Source → Configure → Run & Review workflow.
Rejected: Hiding export until a result object exists | the backend can retain a latest report even when the frontend result is empty.
Rejected: Leaving export notices as generic inline notes | success and failure announcements need deterministic live-region behavior and long path wrapping.
Confidence: high
Scope-risk: narrow
Directive: Keep live monitoring focused on transient runner output; keep retained report export and export notices in the Latest Result report area.
Tested: `npm test -- src/app/App.validation.test.tsx`; `npm run typecheck`; `npm run lint`; `npm run build`; `npm test`; `cd src-tauri && cargo test && cargo check`.
Not-tested: GitHub-hosted release workflow matrix before pushing the tag.

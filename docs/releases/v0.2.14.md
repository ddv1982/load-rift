## Summary

- Publishes the verified release line as Load Rift 0.2.14 with app metadata aligned across npm, Cargo, and Tauri packaging.
- Keeps the Source → Configure → Run & Review workspace introduced in 0.2.13 as the current supported release experience.
- Carries forward retained report export behavior, export notice accessibility, and the k6 v2.0.0 bundled runtime notice without changing application behavior.

## Downloads

- macOS Apple Silicon DMG: attached to this GitHub release as `Load.Rift_0.2.14_aarch64.dmg`
- macOS Intel DMG: attached to this GitHub release as `Load.Rift_0.2.14_x64.dmg`
- Linux AppImage: attached to this GitHub release as `Load.Rift_0.2.14_amd64.AppImage`
- Linux `.deb`: attached to this GitHub release as `Load.Rift_0.2.14_amd64.deb`
- Linux `.rpm`: attached to this GitHub release as `Load.Rift-0.2.14-1.x86_64.rpm`
- APT setup script: attached to this GitHub release as `install-apt-repo.sh`
- APT setup package: attached to this GitHub release as `load-rift-repository-setup_1.0_all.deb`

## Linux APT Install

Enable the Load Rift APT repository once:

```bash
bash <(curl -fsSL https://github.com/ddv1982/load-rift/releases/latest/download/install-apt-repo.sh)
```

Then install or update normally:

```bash
sudo apt update
sudo apt install load-rift
```

The setup script authenticates the repository setup package checksum with a detached GPG signature and a release-pinned signer fingerprint before installing the repository configuration.

## Bundled Third-Party Software

This package bundles Grafana k6 v2.0.0, licensed under AGPL-3.0-only.
Corresponding source: https://github.com/grafana/k6/tree/v2.0.0
Source archive: https://github.com/grafana/k6/archive/refs/tags/v2.0.0.tar.gz
Additional bundled licensing notices are included in the package.

## Verification

- [x] `npm run typecheck`
- [x] `npm run lint`
- [x] `npm run build`
- [x] `npm test`
- [x] `cargo test --manifest-path src-tauri/Cargo.toml`

## Lore

Constraint: Release notes are read by the GitHub Actions release workflow from `docs/releases/v0.2.14.md`.
Constraint: This release is a metadata and release-notes advance from the verified 0.2.13 application line.
Rejected: Re-opening application behavior for this release | no post-0.2.13 code changes were needed before publishing the next release tag.
Rejected: Publishing without a dedicated notes file | the release workflow expects tag-specific notes for the current release line.
Rejected: Reintroducing npm or Rust dependency notice inventories | the bundled third-party notice scope remains focused on the shipped k6 runtime.
Confidence: high
Scope-risk: narrow
Directive: Publish 0.2.14 as the current verified release line with matching package metadata and lore-format notes.
Tested: `npm run typecheck`; `npm run lint`; `npm run build`; `npm test`; `cargo test --manifest-path src-tauri/Cargo.toml`.
Not-tested: GitHub-hosted release workflow matrix before pushing the tag.
